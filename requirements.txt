# Keep direct dependencies exact so a redeploy cannot silently pull a breaking
# release. Refresh these intentionally after running scripts/check.ps1.
fastapi==0.141.1
uvicorn[standard]==0.52.0
pydantic==2.13.4
python-multipart==0.0.32
httpx==0.28.1
psutil==7.2.2
